# fixtures — synthetic test fixtures

Owner: reproducible, minimal, deterministic fixtures for every claimed
capability (RPR-008 and later adapter tasks).

- **Status:** placeholder directory (`RPR-004`). Fixtures are generated by
  scripts, never checked in as real content or disk images; generated media
  stays outside Git (see `.gitignore` and the repository validator).
- **Rule:** fixtures contain no real personal information or live passwords;
  expected results are machine-readable and hash-pinned where applicable.
