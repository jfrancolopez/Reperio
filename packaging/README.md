# packaging — release packaging

Owner: installer, signed OCI images, SBOM/notices, and release artifacts
(`RPR-145`–`RPR-156`).

- **Status:** placeholder directory (`RPR-004`). No images, manifests, or
  release flows exist yet. Priorities: sign/version pin, host profiles, and
  digest-immutable images.
